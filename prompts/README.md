# Prompt assets

Specification v0.5.0 defines the complete instrument contract. The recovered Stage-0 prompt subset currently remains embedded in `stage0/stage0_qwen_to_judge_requests.py` to preserve its validated execution path.

A future scientific change should extract the complete 24-family prompt registry into immutable versioned data only after line-by-line equivalence tests prove that the migration preserves prompt text, opportunity sets, ordering, identifiers, and hashes.
