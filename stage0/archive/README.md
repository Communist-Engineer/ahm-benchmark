# Superseded Stage-0 work

This archive preserves unique pre-v7 patchers, validators, factual-source download iterations, benchmark utilities, and intermediate manifests. Active execution uses the scripts one directory above.

Identical duplicate files from the recovered snapshot remain represented in `data/manifests/source_snapshot_inventory.csv` rather than repeated here. Historical outputs remain in the external source archive and are referenced by filename, size, and SHA-256.
