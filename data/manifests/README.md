# Provenance manifests

The source-snapshot inventory records each recovered file, hash, size, classification, and Git disposition. Manifest-only records preserve traceability for files excluded because they are bulk generated output, third-party source documents, duplicate byte copies, caches, or machine-specific operational artifacts.
